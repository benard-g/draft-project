{
  "name": "project",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "wsrun -at build",
    "build:clean": "wsrun -at build:clean",
    "test": "wsrun -at test"
  },
  "workspaces": {
    "packages": [
      "packages/*/*"
    ],
    "nohoist": [
      "@packages/*",
      "@packages/frontend-*/typescript"
    ]
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
