{
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "wsrun -at build",
    "build:clean": "wsrun -at build:clean",
    "test": "wsrun -at test"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/@packages/*"
    ]
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
